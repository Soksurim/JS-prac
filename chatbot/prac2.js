/* ########################
  이미지 송수신 실험
#########################*/

function response(
  room,
  msg,
  sender,
  isGroupChat,
  replier,
  ImageDB,
  packageName
) {

    print(ImageDB.getProfileImage());
    print(ImageDB.getImage());
}
