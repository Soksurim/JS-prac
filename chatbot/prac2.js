/* ########################
  이미지 송수신 실험
#########################*/

function response(
  room,
  msg,
  sender,
  isGroupChat,
  replier,
  ImageDB,
  packageName
) {
  var rand = Math.floor(Math.random() * 10) / 3;

  console.log(rand);

  // if (msg.include("성공")) {
  //   for (var i = 0; i < rand; i++) {
  //     Utils.delay(100);
  //     replier.reply("*사냥");
  //   }
  // }
}
